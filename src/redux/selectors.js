export const getContacts = state => state.contacts;
export const getFilter = state => state.filter;
export const getNames = state => state.contacts.names;
export const getNumbers = state => state.contacts.numbers;
